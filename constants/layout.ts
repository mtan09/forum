/**
 * Web content column widths.
 *
 * The stack header centers its own row, so it has to be told which column it
 * is sitting above — otherwise Back and the brand drift out to an 840px band
 * while a 500px auth form sits in the middle of the page.
 */
export const WEB_CONTENT_MAX_WIDTH = 840;
export const AUTH_CONTENT_MAX_WIDTH = 500;
